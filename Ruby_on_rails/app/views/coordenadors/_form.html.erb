<%= form_with(model: coordenador) do |form| %>
  <% if coordenador.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(coordenador.errors.count, "error") %> prohibited this coordenador from being saved:</h2>

      <ul>
        <% coordenador.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :departamento_id, style: "display: block" %>
    <%= form.text_field :departamento_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
