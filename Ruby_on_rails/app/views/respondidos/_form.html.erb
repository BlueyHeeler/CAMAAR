<%= form_with(model: respondido) do |form| %>
  <% if respondido.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(respondido.errors.count, "error") %> prohibited this respondido from being saved:</h2>

      <ul>
        <% respondido.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :questionario_id, style: "display: block" %>
    <%= form.text_field :questionario_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
